server:
  port: 8080  # 9000번 포트로 실행

spring:
  application:
    name: fromu

  # DATABASE
  # username : bella
  # password : fromu1212
  # ENDPOINT : fromu-db.cckllmursbot.ap-northeast-2.rds.amazonaws.com
  jpa:
    database: mysql
    show-sql: true
    generate-ddl: true

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://fromu-db.cckllmursbot.ap-northeast-2.rds.amazonaws.com/fromuDB?useSSL=false
    username: bella
    password: fromu1212

cloud:
  aws:
    s3:
      bucket: fromubucket
    credentials:
      access-key: AKIATFFCWWAN6MAYFHUU
      secret-key: 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
    region:
      static: ap-northeast-2
      auto: false
    stack:
      auto: false

logging:
  level:
    com:
      amazonaws:
        util:
          EC2MetadataUtils: ERROR